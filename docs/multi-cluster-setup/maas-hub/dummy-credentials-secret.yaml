# Dummy credentials for ExternalModel
# The internal Envoy endpoint doesn't require authentication
apiVersion: v1
kind: Secret
metadata:
  name: dummy-credentials
  namespace: models-as-a-service
type: Opaque
stringData:
  api-key: "not-needed"
