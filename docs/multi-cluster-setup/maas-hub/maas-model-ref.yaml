# Register ExternalModel with Hub's MaaS
# This enables tenant-level authentication and rate limiting
apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSModelRef
metadata:
  name: hub-model
  namespace: models-as-a-service
spec:
  modelRef:
    kind: ExternalModel
    name: hub-inference-model
