apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: maas-hub-gateway-cert
  namespace: llm-d-system
spec:
  secretName: maas-hub-gateway-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - maas-hub.apps.aigrid-hub.aigriddev.sysdeseng.com
