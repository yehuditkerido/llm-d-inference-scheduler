# ServiceAccount with anyuid SCC for vLLM
# Required because vLLM/PyTorch needs a valid UID in /etc/passwd
# OpenShift uses random UIDs by default which causes getpass.getuser() to fail
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vllm-sa
  namespace: llm-inference
