# Dummy credentials for ExternalModel pointing to internal Envoy
# No actual credentials needed since Envoy is internal
apiVersion: v1
kind: Secret
metadata:
  name: dummy-credentials
  namespace: models-as-a-service
type: Opaque
stringData:
  api-key: "not-used-for-internal-endpoint"
